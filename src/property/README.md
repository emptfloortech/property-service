# Property module

This folder will contain the property domain code (modules, controllers, services, entities).

You can scaffold a NestJS module, controller and service here, for example:

- `property.module.ts`
- `property.controller.ts`
- `property.service.ts`
- `entities/` (for TypeORM entities)

If you want, I can scaffold those files now.
